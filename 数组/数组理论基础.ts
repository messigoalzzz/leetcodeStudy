/** 数组在内存中的存储方式
 * 1.数组是存放在连续内存空间上的相同类型数据的集合。
 * 2.数组可以方便的通过下标索引的方式获取到下标下对应的数据
 */

//字符数组的例子             [h,e,l,l,o]
//内存地址                  100，101，102，103，104，105
//数组索引(下标地址)          0，1，2，3，4，5

/** 注意
 * 数组下标都是从0开始的。
 * 数组内存空间的地址是连续的
 * 正是因为数组的在内存空间的地址是连续的，所以我们在删除或者增添元素的时候，就难免要移动其他元素的地址。
 */
